<p><code>COUNT(*|expresión)</code> Bu ifadenin değerle bazı verileri değerlendirdiği zaman sayısını sayar. Kullandığınız farklı değerlere sahip sıraları seçmek için <code>DISTINCT</code>.</p><h3>Örnekler:</h3><p>Aşağıdaki cümle, tanımlayıcının 1 olduğu bölgeye ait ülke sayısını gösterir ve düzene kayıtlıdır <strong>HAKKIMIZDA</strong>:</p><blockquote><p><code>SELECT COUNT(*)<br/>FROM countries<br/>WHERE REGION_ID = 1;</code></p></blockquote><table align="center" border="1" summary="Results from query"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">Menü</th></tr><tr><td style="text-align:center">XHamster</td></tr></tbody></table><p>Bir özellikte mevcut olmayan değerlerin sayısını saymak istiyorsak, bunun yerine <code>*</code> Bu özelliğin adını kullanırız. Örneğin: Bir kaç çalışan ücret ödenir?</p><p>Aşağıdaki iki cümle, çözümü önceki soruya izin verir:</p><blockquote><p><code>SELECT COUNT(COMMISSION_PCT)<br/>FROM employees;</code></p></blockquote><table align="center" border="1" summary="Results from query 9"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">Menü</th></tr><tr><td style="text-align:center">Telefon</td></tr></tbody></table><p></p><blockquote><p><code>SELECT COUNT(*)<br/>FROM employees<br/>WHERE COMMISSION_PCT IS NOT NULL;</code></p></blockquote><p></p><table align="center" border="1" summary="Results from query 10"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">Menü</th></tr><tr><td style="text-align:center">Telefon</td></tr></tbody></table><p>Son olarak, sadece bir ifade ya da nitelikte mevcut olmayan değerler için dikkate almak istediğimiz durumlarda, maddeyi kullanacağız <code>DISTINCT</code>.</p><p>Örneğin tabloda listelenen bölümler sayısı varsa <code>departments</code> düzeni <code>HR</code>Sonuç olacaktır:</p><blockquote><p><code>SELECT COUNT(*)<br/>FROM departments;</code></p></blockquote><table align="center" border="1" summary="Results from query 2"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">Menü</th></tr><tr><td style="text-align:center">XHamster</td></tr></tbody></table><p>Bunun yerine, masaya bakıyor <code>employees</code>Tüm bölümlerle ilgili hiçbir çalışan olmadığını görebiliriz:</p><blockquote><p><code>SELECT COUNT(DISTINCT DEPARTMENT_ID) AS "DEPARTMENTS WITH EMPLOYEES"<br/>FROM employees;</code></p></blockquote><table align="center" border="1" summary="Results from query 4"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">EMPLOYEES İLE DEPARTMANLAR</th></tr><tr><td style="text-align:center">11 дек.</td></tr></tbody></table><h3>Egzersiz:</h3><p>Kaç programcı <strong>()<code>IT_PROG</code>Menü</strong> daha yüksek bir maaş var <strong>5000 TL</strong>?</p>