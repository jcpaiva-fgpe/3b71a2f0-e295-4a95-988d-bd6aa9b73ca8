<p><code>COUNT(*|expresi&oacute;n)</code> Cuenta el n&uacute;mero de veces que la expresi&oacute;n eval&uacute;a alg&uacute;n dato con valor no nulo. Para seleccionar filas con valores distintos se deber&iacute;a utilizar <code>DISTINCT</code>.</p><h3>Ejemplos:</h3><p>La siguiente sentencia muestra el n&uacute;mero de pa&iacute;ses que pertenecen a la regi&oacute;n cuyo identificador es el 1 y que est&aacute;n registrados en el esquema <strong>HR</strong>:</p><blockquote><p><code>SELECT COUNT(*)<br />FROM countries<br />WHERE REGION_ID = 1;</code></p></blockquote><table align="center" border="1" summary="Results from query"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">8</td></tr></tbody></table><p>Si queremos contar el n&uacute;mero de valores no nulos que existen en un atributo, en lugar del <code>*</code> utilizar&iacute;amos el nombre de ese atributo. Por ejemplo: &iquest;a cu&aacute;ntos empleados se les paga alguna comisi&oacute;n?</p><p>Las dos sentencias siguientes permiten obtener la soluci&oacute;n a la pregunta anterior:</p><blockquote><p><code>SELECT COUNT(COMMISSION_PCT)<br />FROM employees;</code></p></blockquote><table align="center" border="1" summary="Results from query 9"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">35</td></tr></tbody></table><p>&nbsp;</p><blockquote><p><code>SELECT COUNT(*)<br />FROM employees<br />WHERE COMMISSION_PCT IS NOT NULL;</code></p></blockquote><p>&nbsp;</p><table align="center" border="1" summary="Results from query 10"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">35</td></tr></tbody></table><p>Por &uacute;ltimo, en el caso en el que &uacute;nicamente queramos contabilizar los valores no repetidos que existen en un expresi&oacute;n o atributo, utilizar&iacute;amos la cl&aacute;sula <code>DISTINCT</code>.</p><p>Por ejemplo, si contamos el n&uacute;mero de departamentos alamacenados en la tabla <code>departments</code> del esquema <code>HR</code>, el resultado ser&aacute; de:</p><blockquote><p><code>SELECT COUNT(*)<br />FROM departments;</code></p></blockquote><table align="center" border="1" summary="Results from query 2"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">27</td></tr></tbody></table><p>En cambio, mirando en la tabla <code>employees</code>, podemos ver que no hay empleados asociados a todos los departamentos:</p><blockquote><p><code>SELECT COUNT(DISTINCT DEPARTMENT_ID) AS &quot;DEPARTMENTS WITH EMPLOYEES&quot;<br />FROM employees;</code></p></blockquote><table align="center" border="1" summary="Results from query 4"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">DEPARTMENTS WITH EMPLOYEES</th></tr><tr><td style="text-align:center">11</td></tr></tbody></table><h3>Ejercicio:</h3><p>&iquest;Cu&aacute;ntos programadores <strong>(<code>IT_PROG</code>)</strong> tienen un salario superior a <strong>5000</strong>?</p>