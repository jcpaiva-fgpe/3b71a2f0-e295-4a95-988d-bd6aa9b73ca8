<p><code>COUNT(*|expresión)</code> It counts the number of times that the expression evaluates some data with value not null. To select rows with different values you should use <code>DISTINCT</code>.</p><h3>Examples:</h3><p>The following sentence shows the number of countries belonging to the region whose identifier is 1 and are registered in the scheme <strong>HR</strong>:</p><blockquote><p><code>SELECT COUNT(*)<br/>FROM countries<br/>WHERE REGION_ID = 1;</code></p></blockquote><table align="center" border="1" summary="Results from query"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">8</td></tr></tbody></table><p>If we want to count the number of non-existent values that exist in an attribute, instead of the <code>*</code> We'd use the name of that attribute. For example: How many employees are paid a fee?</p><p>The following two sentences allow the solution to the previous question:</p><blockquote><p><code>SELECT COUNT(COMMISSION_PCT)<br/>FROM employees;</code></p></blockquote><table align="center" border="1" summary="Results from query 9"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">35</td></tr></tbody></table><p></p><blockquote><p><code>SELECT COUNT(*)<br/>FROM employees<br/>WHERE COMMISSION_PCT IS NOT NULL;</code></p></blockquote><p></p><table align="center" border="1" summary="Results from query 10"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">35</td></tr></tbody></table><p>Finally, in the case where we only want to account for the non-recurring values that exist in an expression or attribute, we would use the clause <code>DISTINCT</code>.</p><p>For example, if we have the number of departments listed in the table <code>departments</code> of the scheme <code>HR</code>, the result will be:</p><blockquote><p><code>SELECT COUNT(*)<br/>FROM departments;</code></p></blockquote><table align="center" border="1" summary="Results from query 2"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">COUNT</th></tr><tr><td style="text-align:center">27</td></tr></tbody></table><p>Instead, looking at the table <code>employees</code>, we can see that there are no employees associated with all departments:</p><blockquote><p><code>SELECT COUNT(DISTINCT DEPARTMENT_ID) AS "DEPARTMENTS WITH EMPLOYEES"<br/>FROM employees;</code></p></blockquote><table align="center" border="1" summary="Results from query 4"><tbody><tr><th style="background-color:#9e9e9e; text-align:center">DEPARTMENTS WITH EMPLOYEES</th></tr><tr><td style="text-align:center">11</td></tr></tbody></table><h3>Exercise:</h3><p>How many programmers <strong>(<code>IT_PROG</code>)</strong> have a salary higher than <strong>5000</strong>?</p>