<h2><code>INTERSECT</code></h2><p>Operasyonda <code>INTERSECT</code>iki rütbeleri incelenir <code>SELECT</code> ve her iki sette görünenler geri dönülecektir. Yinelenen satırlar kaldırılacaktır.</p><h3>Türkçe</h3><blockquote><p><code>SELECT query</code></p></blockquote><p><code>INTERSECT</code></p><p><code>SELECT query</code></p><p>Hem danışmalar dönen sütun sayısı <code>SELECT</code> Aynı olmalı ve bu tür sütun türleri uyumlu olmalıdır.</p><h3>Örnek:</h3><p>Aynı bölümde zaten geliştirmiş olduklarından başka bir iş yapan çalışanları gösterir:</p><blockquote><p><code>SELECT DISTINCT FIRST_NAME, LAST_NAME, DEPARTMENT_ID<br/>FROM employees<br/>INTERSECT<br/>SELECT DISTINCT FIRST_NAME, LAST_NAME, job_history.DEPARTMENT_ID<br/>FROM job_history JOIN employees USING (EMPLOYEE_ID);</code></p></blockquote><table align="center" border="1" summary="Results from query"><tbody><tr><th style="text-align: center; background-color: rgb(158, 158, 158);">FIRST_NAME</th><th style="text-align: center; background-color: rgb(158, 158, 158);">E-Mail _ Sitemap</th><th style="text-align: center; background-color: rgb(158, 158, 158);">DEPARTMENT_ID</th></tr><tr><td>xHamster</td><td>xHamster</td><td>0,00 TL</td></tr><tr><td>Menü</td><td>Menü</td><td>Telefon</td></tr><tr><td>Michael.</td><td>Türkçe</td><td>Telefon: +49 (0)</td></tr><tr><td>Menü</td><td>Türkçe</td><td>Telefon</td></tr></tbody></table><h3>Egzersiz</h3><p><code>Neena Kochhar</code> mevcut bölümde rahat değil, bu nedenle hareket etmeyi düşünen <code>'UK'</code>.</p><p>Tanımlayıcısı olan ülkede bulunan bir departmanı bulmanız gerekir <code>'UK'</code> ve ortalama ücret 8000'den fazla</p><p>Menü <code>INTERSECT</code> yardım etmek <code>Neena Kochhar</code> İhtiyacınız olan departmanı bulmak için</p>