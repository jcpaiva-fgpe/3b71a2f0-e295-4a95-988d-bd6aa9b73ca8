<p>Finally, the operator <code>NOT </code>return <code>TRUE </code>if the logical expression with which it operates returns <code>FALSE</code>.</p><h3>Example:</h3><p>The following consultation will show countries that do not belong to the region whose identifier is 1.</p><blockquote><p><code>SELECT COUNTRY_NAME, REGION_ID<br/>FROM countries<br/>WHERE NOT REGION_ID = 1;</code></p></blockquote><table align="center" border="1" summary="Results from query"><tbody><tr><th style="background-color:#9e9e9e">COUNTRY_NAME</th><th style="background-color:#9e9e9e">REGION_ID</th></tr><tr><td>Argentina</td><td>2</td></tr><tr><td>Australia</td><td>3</td></tr><tr><td>Brazil</td><td>2</td></tr><tr><td>Canada</td><td>2</td></tr><tr><td>China</td><td>3</td></tr><tr><td>Egypt</td><td>4</td></tr><tr><td>...</td><td>...</td></tr></tbody></table><p>It is true that the previous exercise could also have been resolved with a comparison operator:</p><blockquote><p><code>SELECT COUNTRY_NAME, REGION_ID<br/>FROM countries<br/>WHERE REGION_ID <> 1;</code></p></blockquote><h3>Exercise:</h3><p>Using the operator <code>NOT</code>shows the names (<code>JOB_TITLE</code>) of the works (<code>jobs</code>) in which the difference between the highest pay(<code>MAX_SALARY</code>) and the lowest wage(<code>MIN_SALARY</code>It's, at least, <strong>10000</strong>.</p><p>It orders the results from which the lower wage difference allows to the one that allows the greatest wage difference.</p>