<p>En los ejercicios anteriores, los datos resultantes de la ejecuci&oacute;n de las sentencias se mostraban en el orden en el que fueron introducidos en la tabla. Si queremos que los datos aparezcan ordenados seg&uacute;n el valor de alguno de sus atributos, a&ntilde;adiremos la cla&uacute;sula <code>ORDER BY</code>, siguiendo la siguiente sintaxis:</p><blockquote><p><code>SELECT [<em>atributos</em>]<br />FROM [<em>tabla</em>]<br />ORDER BY [<em>atributos</em>];</code></p></blockquote><h3>Ejemplo:</h3><p>El resultado de ejecutar la sentencia siguiente:</p><blockquote><p><code>SELECT * FROM<br />regions;</code></p></blockquote><p>es:</p><table align="center" border="1px solid" summary="Results from query"><tbody><tr><th>REGION_ID</th><th>REGION_NAME</th></tr><tr><td>1</td><td>Europe</td></tr><tr><td>2</td><td>Americas</td></tr><tr><td>3</td><td>Asia</td></tr><tr><td>4</td><td>Middle East and Africa</td></tr></tbody></table><p>En cambio, si queremos que el resultado se muestre ordenado por el nombre de la region (<code>REGION_NAME</code>), le tendremos que a&ntilde;adir la cl&aacute;usula <code>ORDER BY</code>:</p><blockquote><p><code>SELECT * FROM<br />regions<br />ORDER BY REGION_NAME;</code></p></blockquote><table align="center" border="1px solid" summary="Results from query"><tbody><tr><th>REGION_ID</th><th>REGION_NAME</th></tr><tr><td>2</td><td>Americas</td></tr><tr><td>3</td><td>Asia</td></tr><tr><td>1</td><td>Europe</td></tr><tr><td>4</td><td>Middle East and Africa</td></tr></tbody></table><h3>Ejercicio:</h3><p>Muestra los nombre (<code>DEPARTMENT_NAME</code>) de los departamentos (<code>departments</code>) ordenados por el nombre</p>