<p>Önceki yıllarda, cümlelerin uygulanmasından kaynaklanan veriler tabloda tanıtıldığı sırada gösterilmiştir. Verileri, herhangi bir özelliğin değerine göre sıralanmış görünmesini istiyorsak, madde ekleyeceğiz <code>ORDER BY</code>aşağıdaki sözdizimi takip edin:</p><blockquote><p><code>SELECT [<em>atributos</em>]<br/>FROM [<em>tabla</em>]<br/>ORDER BY [<em>atributos</em>];</code></p></blockquote><h3>Örnek:</h3><p>Aşağıdaki cümlenin uygulanmasının sonucu:</p><blockquote><p><code>SELECT * FROM<br/>regions;</code></p></blockquote><p>var:</p><table align="center" border="1px solid" summary="Results from query"><tbody><tr><th>REGION_ID</th><th>REGION_NAME</th></tr><tr><td>1 XHamster</td><td>Avrupa</td></tr><tr><td>2 XHamster</td><td>Türkçe</td></tr><tr><td>XHamster</td><td>Türkçe</td></tr><tr><td>XHamster</td><td>Orta Doğu ve Afrika</td></tr></tbody></table><p>Bunun yerine, bölgenin adı ile ordained görünmesini istiyorsak (<code>REGION_NAME</code>), biz madde eklemeniz gerekecek <code>ORDER BY</code>:</p><blockquote><p><code>SELECT * FROM<br/>regions<br/>ORDER BY REGION_NAME;</code></p></blockquote><table align="center" border="1px solid" summary="Results from query"><tbody><tr><th>REGION_ID</th><th>REGION_NAME</th></tr><tr><td>2 XHamster</td><td>Türkçe</td></tr><tr><td>XHamster</td><td>Türkçe</td></tr><tr><td>1 XHamster</td><td>Avrupa</td></tr><tr><td>XHamster</td><td>Orta Doğu ve Afrika</td></tr></tbody></table><h3>Egzersiz:</h3><p>Adı göster ()<code>DEPARTMENT_NAME</code>Bölümler ()<code>departments</code>) isim ile sıralama</p>