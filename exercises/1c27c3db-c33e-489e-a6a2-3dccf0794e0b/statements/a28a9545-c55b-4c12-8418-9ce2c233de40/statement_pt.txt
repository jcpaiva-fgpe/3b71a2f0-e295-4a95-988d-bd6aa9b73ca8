<p>Nos anos anteriores, os dados resultantes da execução das sentenças foram mostrados na ordem em que foram introduzidos na tabela. Se quisermos que os dados apareçam classificados de acordo com o valor de qualquer um dos seus atributos, adicionaremos a cláusula <code>ORDER BY</code>seguindo a seguinte sintaxe:</p><blockquote><p><code>SELECT [<em>atributos</em>]<br/>FROM [<em>tabla</em>]<br/>ORDER BY [<em>atributos</em>];</code></p></blockquote><h3>Exemplo:</h3><p>O resultado da execução da seguinte frase:</p><blockquote><p><code>SELECT * FROM<br/>regions;</code></p></blockquote><p>é:</p><table align="center" border="1px solid" summary="Results from query"><tbody><tr><th>REGION_ID</th><th>REGION_NAME</th></tr><tr><td>1</td><td>Europa</td></tr><tr><td>2</td><td>América do Sul</td></tr><tr><td>3</td><td>Ásia</td></tr><tr><td>4</td><td>Oriente Médio e África</td></tr></tbody></table><p>Em vez disso, se quisermos que o resultado seja exibido pelo nome da região (<code>REGION_NAME</code>), teremos que adicionar a cláusula <code>ORDER BY</code>:</p><blockquote><p><code>SELECT * FROM<br/>regions<br/>ORDER BY REGION_NAME;</code></p></blockquote><table align="center" border="1px solid" summary="Results from query"><tbody><tr><th>REGION_ID</th><th>REGION_NAME</th></tr><tr><td>2</td><td>América do Sul</td></tr><tr><td>3</td><td>Ásia</td></tr><tr><td>1</td><td>Europa</td></tr><tr><td>4</td><td>Oriente Médio e África</td></tr></tbody></table><h3>Exercício:</h3><p>Mostrar o nome (<code>DEPARTMENT_NAME</code>) dos departamentos (<code>departments</code>) ordenada pelo nome</p>