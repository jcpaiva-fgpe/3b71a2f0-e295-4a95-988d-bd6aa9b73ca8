<p>To manipulate existing data in a <strong>relational database</strong>, let's use the language <strong><big>SQL</big></strong>. In particular, if what we want is to extract a subset of the existing data, we will use the command <code>SELECT</code>.</p><p>The first thing we're going to do is show all the data in a given table, without any restrictions.</p><h3>Example:</h3><p>if we want to show all the data in the table <code>regions</code> We'll do it.</p><blockquote><p><code>SELECT * FROM regions;</code></p></blockquote><p>All we need to do is write the name of the table (<code>regions</code>) behind SQL text <code>SELECT * FROM</code>, obtaining the following table:</p><table border="1px solid blue"><tbody><tr><th>REGION_ID</th><th>REGION_NAME</th></tr><tr><td>1</td><td>Europe</td></tr><tr><td>2</td><td>Americas</td></tr><tr><td>3</td><td>Asia</td></tr><tr><td>4</td><td>Middle East and Africa</td></tr></tbody></table><h3>Exercise:</h3><p>According to the example above, it shows all the data in the table <code>countries</code>. Write your solution in the corresponding section and choose <strong>SQL-DQL</strong></p><p>The result should be similar to that shown in the TESTS section.</p>