<h3>Ejemplo:</h3><p>La funci&oacute;n <code>replace ( string text, from text, to text ) → text</code> devuelve la cadena_original en la que cada ocurrencia de <code>from text</code> es remplazada por <code>to text</code>.</p><p><code>SELECT REPLACE(&#39;JACK and JUE&#39;,&#39;J&#39;,&#39;BL&#39;) &quot;Changed&quot;;</code></p><table align="center" border="1" summary="Results from query 15"><tbody><tr><th style="background-color: rgb(158, 158, 158);">Changed</th></tr><tr><td>BLACK and BLUE</td></tr></tbody></table><h3>Ejercicio:</h3><p>Muestra los empleados que tienen un salario entre 4000 y 6000, de forma que aquellos que tengan asignado el trabajo <code>&#39;IT_PROG&#39;</code> aparezcan con el trabajo <code>&#39;Programmer&#39;</code>.</p><p>Ordena el resultado alfab&eacute;ticamente por el apellido.</p>