<h3>Example:</h3><p>Function <code>replace ( string text, from text, to text ) → text</code> returns the_original chain in which each occurrence of <code>from text</code> replaced by <code>to text</code>.</p><p><code>SELECT REPLACE('JACK and JUE','J','BL') "Changed";</code></p><table align="center" border="1" summary="Results from query 15"><tbody><tr><th style="background-color: rgb(158, 158, 158);">Changed</th></tr><tr><td>BLACK and BLUE</td></tr></tbody></table><h3>Exercise:</h3><p>It shows employees who have a salary between 4000 and 6000, so that those who have assigned the job <code>'IT_PROG'</code> appear with work <code>'Programmer'</code>.</p><p>Order the result alphabetically by the last name.</p>