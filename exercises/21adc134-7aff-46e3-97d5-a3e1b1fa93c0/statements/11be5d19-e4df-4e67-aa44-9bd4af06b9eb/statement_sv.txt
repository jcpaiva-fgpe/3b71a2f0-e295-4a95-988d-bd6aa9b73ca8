<h3>Exempel:</h3><p>Funktion <code>replace ( string text, from text, to text ) → text</code> returnerar ursprungskedjan där varje förekomst av <code>from text</code> ersättas med <code>to text</code>.</p><p><code>SELECT REPLACE('JACK and JUE','J','BL') "Changed";</code></p><table align="center" border="1" summary="Results from query 15"><tbody><tr><th style="background-color: rgb(158, 158, 158);">Ändrad</th></tr><tr><td>BLACK och BLUE</td></tr></tbody></table><h3>Övning:</h3><p>Det visar anställda som har en lön mellan 4000 och 6000, så att de som har tilldelat jobbet. <code>'IT_PROG'</code> verkar med arbete <code>'Programmer'</code>.</p><p>Beställ resultatet alfabetiskt av efternamnet.</p>