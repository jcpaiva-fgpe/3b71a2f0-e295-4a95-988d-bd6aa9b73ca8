<p>'Rome' veya 'IT_PROG' durumunda olduğu için karakter dizlerinin sabitlerini kullanmaya başladığımızda, basit teklifler arasında kilitliyoruz.</p><p>Bunun yerine, sayısal sabitler ise, bunları tırnak işaretlerine koymak gerekli değildir.</p><h3>Egzersiz:</h3><p>Aslında, düzendeki mevcut tüm programlayıcılar <code>HR</code> Bölüme ait <code>60</code>. Bu nedenle önceden egzersiz danışmanlığını hafifçe değiştirelim:</p><p>şemadaki tüm çalışanlar arasında <code>HR</code>, adını gösterir (<code>FIRST_NAME</code>), soyadı (<code>LAST_NAME</code>) ve maaş (<code>SALARY</code>) departmanına ait olanlar (<code>DEPARTMENT_ID</code>60, en az ücretleri birinden sipariş.</p><p>karakter dizlerinin sabitlerini kullanmaya başladığımızda ısrar etmeliyiz, çünkü <code>'Roma'</code> veya <code>'IT_PROG'</code>Basit teklifler arasında kilitleyeceğiz.</p><p>Bunun yerine, sayısal sabitler ise, bunları tırnak işaretlerine koymak gerekli değildir.</p><h3>Egzersiz:</h3><p>Aslında, düzendeki mevcut tüm programlayıcılar <code>HR</code> Bölüme ait <code>60</code>. Bu nedenle önceden egzersiz danışmanlığını hafifçe değiştirelim:</p><p>şemadaki tüm çalışanlar arasında <code>HR</code>, adını gösterir (<code>FIRST_NAME</code>), soyadı (<code>LAST_NAME</code>) ve maaş (<code>SALARY</code>) departmanına ait olanlar (<code>DEPARTMENT_ID</code>60, en az ücretleri birinden sipariş.</p>