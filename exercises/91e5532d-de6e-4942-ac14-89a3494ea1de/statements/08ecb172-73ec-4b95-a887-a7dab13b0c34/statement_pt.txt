<p>Devemos insistir que, sempre que vamos usar constantes de caracteres, como foi o caso de 'Roma' ou 'IT_PROG', nós os bloqueamos entre citações simples.</p><p>Em vez disso, se for constante numérica, não é necessário colocá-los em marcas de cotação.</p><h3>Exercício:</h3><p>Na verdade, todos os programadores existentes no regime <code>HR</code> pertencem ao departamento <code>60</code>. Então vamos modificar ligeiramente a consulta prévia do exercício:</p><p>De todos os empregados no esquema <code>HR</code>, mostra o nome (<code>FIRST_NAME</code>), sobrenomes (<code>LAST_NAME</code>) e salário (<code>SALARY</code>) dos que pertencem ao departamento (<code>DEPARTMENT_ID</code>60, encomendado pelo menos.</p><p>Devemos insistir que sempre que vamos usar constantes de caracteres, como foi o caso de <code>'Roma'</code> ou <code>'IT_PROG'</code>Vamos trancá-los entre citações simples.</p><p>Em vez disso, se for constante numérica, não é necessário colocá-los em marcas de cotação.</p><h3>Exercício:</h3><p>Na verdade, todos os programadores existentes no regime <code>HR</code> pertencem ao departamento <code>60</code>. Então vamos modificar ligeiramente a consulta prévia do exercício:</p><p>De todos os empregados no esquema <code>HR</code>, mostra o nome (<code>FIRST_NAME</code>), sobrenomes (<code>LAST_NAME</code>) e salário (<code>SALARY</code>) dos que pertencem ao departamento (<code>DEPARTMENT_ID</code>60, encomendado pelo menos.</p>